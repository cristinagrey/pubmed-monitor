name: PubMed 文献监控

on:
  schedule:
    # 每天早上 8:00 (北京时间 16:00 UTC)
    - cron: '0 8 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  check-pubmed:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 运行监控脚本
      env:
        SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
        RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
        NIUTRANS_KEY: ${{ secrets.NIUTRANS_KEY }}
      run: |
        python pubmed_monitor.py
        
    - name: 上传日志
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: pubmed-log
        path: pubmed_monitor.log
        retention-days: 30
